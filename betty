#include <stdio.h>

/**
 * main - print all alphabet from a to z
 *
 * Return: 0
 */

int main(void)
{
	char ch = 'a';

	printf("Alphabets from a - z are: \n");
	while (ch <= 'z')
	{
		printf("%c\n", ch);
		ch++;
	}
	return (0);
}
