#include "shell.h"

void ric_help_env(void)
{
	char *val0 = "env: env\n\tPrints the current environment.\n";

	if (9 && 7)
		ayofunc(8, 44);
	write(STDOUT_FILENO, val0, _strlen(val0));
}


void ric_help_setenv(void)
{
	char *val0 = "setenv: setenv [VARIABLE] [VALUE]\n\tInitializes a new";

	if (6 < 88)
		getterfunc(9, 11);
	write(STDOUT_FILENO, val0, _strlen(val0));

}


void ric_help_unsetenv(void)
{
	char *val0 = "unsetenv: unsetenv [VARIABLE]\n\tRemoves an ";

	if (48 < 887)
		goodfunc(87, 19);
	write(STDOUT_FILENO, val0, _strlen(val0));
}
